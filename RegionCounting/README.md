# Region Counting

Counts the people inside a specified region (the sweet spot of the embodied agent).

There are currently two implementations of the region counting model:
- workflow.py: Counts all people inside the frame
- workflow-facing-agent.py: Counts only people inside the frame that are facing the camera

